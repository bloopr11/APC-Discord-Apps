[phases.setup]
nixPkgs = ["...", "git", "python312", "python312Packages.pip"]

[phases.install]
cmds = [
  "python3 -m pip install -r requirements.txt",
  "python3 -m pip install git+https://github.com/StreamAlpha/tvdatafeed.git"
]

[start]
cmd = "python3 app.py"
